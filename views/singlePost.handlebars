{{>postInfo post}}
{{#if loggedIn}}
<form class="post-card">
    <div>
        <textarea name="comment-body" id="comment-body" placeholder="add comment here"></textarea>
    </div>
    <div>
        <button type="submit" class="btn btn-warning comment-form">add comment</button>
    </div>
</form>

{{/if}}

{{> comment post.comment}}

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}}